import React from 'react';
import Form from './Form';

export default {
    title: 'Components / Form',
    component: Form
};

const data = {};

export const Editing = () => {
    return <Form />;
};
